#include <stdio.h>

int main() {
    int numero;
    int fact ;
    printf("escribe un numero entero no negativo: ");
    scanf("%d", &numero);
    int resultado =fact(numero);
        printf("El fact de %d es : %d",número,resultado);

    return 0;
}
}